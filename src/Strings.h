#ifndef STRINGS_H
#define STRINGS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *convertIntToStr(int num);
char *concat(char *s1, char *s2);
char *createString(char *str);

#endif // STRINGS_H
